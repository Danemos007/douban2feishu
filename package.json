{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.ts": [
      "cd backend && npx eslint --fix",
      "cd backend && npx prettier --write"
    ]
  }
}
