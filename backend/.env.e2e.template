# E2E测试环境变量模板
# 复制此文件为 .env.e2e 并填入真实值（仅用于本地E2E测试）

# 测试环境标识
NODE_ENV=test
IS_E2E_TEST=true

# Mock模式开关（CI环境建议启用）
USE_MOCK_DOUBAN=true
USE_MOCK_FEISHU=true

# 测试用户凭证（仅CI环境需要）
E2E_TEST_EMAIL=e2e-test@example.com
E2E_TEST_PASSWORD=test-password-changeme

# 数据库配置（测试数据库）
DATABASE_URL=postgresql://user:password@localhost:5432/d2f_test
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=1

# JWT配置
JWT_SECRET=test-jwt-secret-changeme
JWT_EXPIRATION=1h

# 豆瓣配置（Mock模式下不需要真实值）
DOUBAN_COOKIE=test-cookie

# 飞书配置（Mock模式下不需要真实值）
FEISHU_APP_ID=cli_test123456
FEISHU_APP_SECRET=test-secret
FEISHU_APP_TOKEN=test-token
