# 集成测试环境变量模板
# 复制此文件为 .env.integration 并填入真实值（仅用于本地集成测试）

# 测试环境标识
NODE_ENV=test
IS_INTEGRATION_TEST=true

# Mock模式开关（CI环境建议启用）
USE_MOCK_DOUBAN=true
USE_MOCK_FEISHU=true

# 测试用户凭证（仅CI环境需要）
INTEGRATION_TEST_EMAIL=integration-test@example.com
INTEGRATION_TEST_PASSWORD=test-password-changeme

# 数据库配置（Docker测试数据库）
# 使用 docker-compose.test.yml 启动测试数据库: docker-compose -f docker-compose.test.yml up -d
DATABASE_URL=postgresql://d2f_test_user:d2f_test_password@localhost:5433/d2f_test
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_DB=0

# JWT配置
JWT_SECRET=test-jwt-secret-changeme
JWT_EXPIRATION=1h

# 豆瓣配置（Mock模式下不需要真实值）
DOUBAN_COOKIE=test-cookie

# 飞书配置（Mock模式下不需要真实值）
FEISHU_APP_ID=cli_test123456
FEISHU_APP_SECRET=test-secret
FEISHU_APP_TOKEN=test-token
